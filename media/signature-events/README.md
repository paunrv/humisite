# Media curada — Experiencias HUMI

This folder is not served to the site.

Add new Signature Event assets under:

`public/signature-events/<year>/`

Then list them on the event in `src/lib/signature-events.ts`.
